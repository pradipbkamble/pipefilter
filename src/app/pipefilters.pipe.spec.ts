import { PipefiltersPipe } from './pipefilters.pipe';

describe('PipefiltersPipe', () => {
  it('create an instance', () => {
    const pipe = new PipefiltersPipe();
    expect(pipe).toBeTruthy();
  });
});
